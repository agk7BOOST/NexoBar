namespace NexoBar.IdentitiesAndCapabilities;

/// <summary>
/// Server-owned endpoint metadata: authenticate normally without renewing inactivity.
/// Used for automatic, passive connections rather than ordinary application requests.
/// </summary>
public sealed class PassiveSessionRequest;
